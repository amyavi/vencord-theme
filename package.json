{
  "name": "vencord-theme",
  "version": "0.0.0",
  "description": "personal vencord theme",
  "private": true,
  "devDependencies": {
    "rimraf": "~6.1.2",
    "sass": "~1.97.0"
  },
  "scripts": {
    "build": "sass --style=compressed --silence-deprecation=import --no-source-map src/main.scss:build/theme.css",
    "clean": "rimraf build"
  }
}