{
  "name": "vencord-theme",
  "version": "0.0.0",
  "description": "personal vencord theme",
  "private": true,
  "devDependencies": {
    "rimraf": "~6.0.1",
    "sass": "~1.77.8"
  },
  "scripts": {
    "build": "sass --style=compressed --no-source-map --update src/main.scss:build/theme.css",
    "clean": "rimraf build"
  }
}